import { Resolver } from '@nestjs/graphql';

@Resolver()
export class Erc20Resolver {
  //  async name(parent: ERC20) {
  //         return await parent.contract.name();
  //     },
  //     async symbol(parent: ERC20) {
  //         return await parent.contract.symbol();
  //     },
  //     async decimals(parent: ERC20) {
  //         return await parent.contract.decimals();
  //     },
}
